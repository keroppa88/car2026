# CAR2 LOOP DRIVE

`map.gltf` の高さ0にある濃いグレーの区域を、`car` の運転処理で走行する版です。
地図は元データの75%サイズで表示します（車両サイズは変更しません）。

- `PLAY_CAR2.bat` をダブルクリックして起動
- `S`: アクセル / `A`: ブレーキ
- `←` `→`: ハンドル / `↑` `↓`: シフト
- `Space`: サイドブレーキ
- `M`: 音楽選択(musiclist.txt の「」内を↑↓で選びEnterで再生。運転は一時停止。音量スライダー付き)
- `J`/`K`/`L`: 曲の10秒巻き戻し/再生・一時停止/10秒早送り(運転中も有効)
- `I`/`O`: 前の曲/次の曲(運転中も有効)
- `N`: 夜間モード(全車のヘッドライト・テールランプが点灯) / `B`: 音声切替(エンジン音→車内音→無音)
- `V`: カメラ切替(後方→ボンネット→視点のみ[車体非表示・マウスで視点操作]) / `G`: 表示切替5状態(操作表+音楽+スピード→音楽+スピード→スピードのみ→音楽のみ→なし)
- `F`: バックミラー / `Y`: 自動運転(平均130km/h・急カーブはドリフト) / `ESC`: 一時停止
- ゲームコントローラー対応: 左スティック=ハンドル / RT=アクセル / LT=ブレーキ / A=ドリフト
  / Y=シフトダウン / B=シフトアップ / X=カメラ切替(Vと同じ) / LB=自動運転(Yと同じ) / RB=夜間モード(Nと同じ)
- タイトル画面右下の「説明」ボタンでゲーム紹介画像を表示(何かキー/クリックで閉じる)
- スマホ(UAで判定)は **PC PLAY ONLY**: タイトル画面の操作は無効化され、5秒後にデモ画面へ
  自動遷移する。デモ中はスワイプでカメラ移動、タップでカメラ視点切替(V相当)のみ可能
- 南北の道路端はつながっており、一本道をそのまま走ると周回します
- ループ地点の手前から、道路の先に次の周回地図が連続して見えます
- 高さ0より上の壁・建物に当たり判定があります
- car側の緑色の補助床はcar2では表示しません

ローカルサーバーの既定URLは `http://127.0.0.1:8765/` です。使用中の場合は次の空きポートを自動で使います。

---

# 2026-07-29〜30 実装分（未整理・情報の置き場）

## CPU車の登場リストとランク

- 車種リストは **`登場cpu車両list.txt` が正**。「ファイル名 ランク」を1行ずつ。区切りは半角/全角スペース両対応（15行目が全角だった）
- ユーザーがこのファイルを随時書き換える前提。台数・車種をコードに固定しない
- **配置はリスト順**（周回するので速度順に並べ替えない）。txt由来のときだけ名前順ソートを外している
- 同ランクでも**リスト上位ほど0.5%/行だけ速い**（`CAR2_CPU_LIST_SPEED_STEP`）。完全に同速だと追い越し条件を満たせず数珠つなぎになるため
- 順序を検証するときは必ず現物のtxtを読み直すこと（会話中の古い並びで誤診しかけた）

ランク別の最高速度（ユーザー車180km/h基準／首都高・海岸線）:

| ランク | 倍率  | 速度       |
| --- | --- | -------- |
| S   | 90% | 162 km/h |
| A   | 70% | 126 km/h |
| B   | 65% | 117 km/h |
| C   | 55% | 99 km/h  |

- Sは当初100%だったが、自動運転130km/h巡航で追いつけないため-10%
- Aは当初80%(144)。130km/hより速くBCしか出会えなかったため-30%(126)へ

インディはユーザー車が250km/h基準。さらに **S=-10% / A=-5%** の追加調整（Aを-10%にするとBより遅くなり逆転する）。実測 S 212〜204 / A 170,166 / B 168,166 / C 143,139。

## 首都高速

### 「カーブ手前で震えて固まる」の真因（解決済み）

- 原因は**道路外検出→走行ラインへ強制復帰のループ**。毎フレーム復帰と逸脱を繰り返して震える
- 対処は `skipRoadRecovery: true`。海岸線・インディは以前から同じ扱いで、首都高だけ残っていた
- **Y座標基準のズレは原因ではない**（首都高の地面に高低差はない）。`isCar2RoadAt` にY基準引数を足す案は試して撤回。再提案しない
- 引っ掛かりは首都高のみの問題。他マップへ変更を広げない

### 交通の作り方＝出現タイマー方式

密度と速度差から間接的に作る方式は全部ボツ（頻度が読めず数珠つなぎも避けられない）。

- **最大15台をつくり置き**し、走らせるのは出番が来た車だけ（非アクティブは非表示＋更新スキップ）
- **平均10秒(5〜25秒・指数分布)ごとに1台、ランダムに選んで出現**
- 出現位置：ユーザー車より遅い車は**前方520〜820m**（フォグnear500で霞むので湧いて見えない）、速い車は**後方160〜320m**
- 前方1150m/後方700mで退場、次の出番待ちへ
- 出現間隔は平均速度で案分（150km/h帯=×1.0 / 130km/h帯=×150/130 / 110km/h帯=×150/110）。「平均100km/hはない」ので下は考えない
- スタート時は**90m・220m・380m**にB/Cを**交互に**3台（遅い順に取るとCばかりになる）

### 直線5レーン（ランク別）

道路幅は車5台分＝約9m。中心線を0として1.8m刻み（`CAR2_LANE_SPACING`、`?laneSpacing=`で変更可）。

| レーン  | 位置    | 走る車                |
| ---- | ----- | ------------------ |
| 最も左  | −3.6m | C                  |
| 中央の左 | −1.8m | Bが詰まったとき           |
| 中央   | 0     | **ユーザー車** / B・Aの通常 |
| 中央の右 | +1.8m | Aが詰まったとき           |
| 最も右  | +3.6m | S                  |

- C・Sは追い越し時に中央へ出るが、**ユーザー車が70m以内なら中央を空けて1本内側**に留まる
- 前をふさぐ車の判定に**ユーザー車も含める**
- カーブでは全員が中心線へ戻る（`straightness < 0.5`）。端レーンでは膨らみ代を0.9mに絞る
- 30度より大きい曲がり角だけドリフト（該当26点。45度前後が大半）

### コーナー速度（絶対値キャップ）

`car2CornerCapKmh` — 先の数点の曲がり角の合計で種類を判定。

| カーブ              | ドリフト     | グリップ |
| ---------------- | -------- | ---- |
| U字（窓+7で合計137度以上） | 100 km/h | 110  |
| 垂直（窓+4で69度以上）    | 130      | 150  |
| 単発45度など          | 150      | 170  |

U字は窓+7でだけ検出できる（idx33-35, 209-223などの合計180度区間）。

## インディアナポリス

- **常に10台が周回**。出現タイマーは使わない（コース全体が見えるため）。車種はtxtの上から10台、一周767mに均等配置（車間約70m）
- バンク幅は**33〜45m（平均39m）**。基準線は外側34%＝ユーザー車の位置（「バンク中央より外」は既にその通りだった）
- CPUはそこから**内側6.3m＝バンク中央**が基本ライン。レーン間隔2.2m
- レーンは**譲る動きを入れず、抜く側が動く**：前が詰まる→外が空いていれば外、塞がっていれば内、両方詰まればさらに外
- **`INDY_DRIFT_CHANCE = 1`＝全車が必ずコーナーでドリフト。ドリフトしないのは直線のみ**
- コーナー速度：グリップ205km/h(`INDY_CORNER_GRIP_KMH`) / **ドリフト129km/h**(`INDY_CORNER_DRIFT_KMH`、152から一律15%減)
- **加速性能はユーザー車と同じ。超過は絶対禁止**。`playerLikeMaxAccel(v)` = 最高段ギアの `acc×1.5×(1-v/vmax)` を上限にクランプ。従来の「速度差×1.6」だとドリフト明けに37m/s²が出てユーザー車上限21m/s²を超えていた（実測11.0m/s²に収まった）

### ワープ・突然現れるの2つの真因（解決済み）

1. **周回の継ぎ目で毎周 pos.set していた**（座標と向きを強制書き換え）→ `closedLoop: true` でidxだけ巻き戻す。座標は触らない
2. **走行ラインへのクランプが継ぎ目をまたげなかった**。ルート生成でレイの外れた点が捨てられ終端と先頭の間に約16mの隙間があり、idx=0に戻った瞬間に車が14m前へスナップ（毎周発生）→ クランプのセグメント探索をclosedLoopでは剰余で回して継ぎ目も見る

**ワープ検出器を常設**：1フレームの移動が (v+10)×dt×2+1m を超えると `cpuMaxJumpMeters` / `cpuMaxJumpInfo` に記録。意図した瞬間移動は `ai.stepPrevX = undefined` でリセット。修正後は0.0。

### カーブ減速が効かなかった真因

減速式が「次ウェイポイントへの角度差(diff)」基準だが、インディは経路点が6m間隔と密でカーブ中もdiffがほぼ0。`cornerSlowdown` をいくら上げても効かない。→ **5点先読みの indyCurve を使う**。

## CPU同士はすり抜ける（首都高・インディ共通）

- **CPU同士に衝突判定も車間維持の減速も入れない**。抜けない場面はそのまますり抜けさせる
- 前車に速度を合わせると、抜けない車が次々に後ろへ連なって数珠つなぎになる。見た目の重なりより数珠つなぎの方が問題
- 追い越し（レーン移動）のロジックは残す
- レーン変更は**最低1.5秒保つ**（`CPU_LANE_HOLD_S`）。毎フレーム決め直すと左右にフラつく（入れる前は45秒で554回→54回）
- 横移動は `CPU_LANE_CHANGE_RATE = 1.2`。3だと横へワープしたように見えた

## 海岸線

- **対向7 : 同方向3**（20台なら対向14・同方向6）。総交通量20台
- 速度はランク基準。**対向車だけ一律50%ダウン**（同方向 S162/A126/B117/C99、対向 S81/A63/B59/C50）
- `game-config.js` の `cpuOncomingSpeedsKmh`/`cpuSameDirectionSpeedsKmh` はもう使っていない
- **CPU車とユーザー車は相互にすり抜ける**（交通量優先。`COURSE_KEY !== 'sea'` で衝突判定をスキップ）
- レーンX固定：同方向66.6〜69.1(実67.675) / 対向71.1〜73.8(実72.475)

## 当たり判定（ユーザー車 ↔ CPU車）

- CPU車は**向き付き長方形**（普通車1.9×4.2m＝車体2.1×4.6よりやや小さめ、バイク0.64×1.8）。ユーザー車は前後±1.4mの2円(半径1.05)で近似し、入った分だけ押し出す（`collideCarRect`）
- 円1個だった頃は前後1.2mがすり抜けていた（「当たり判定が少なすぎる」の原因）
- **出番待ち(非アクティブ)の車は判定から除外**。位置が残るので、除外しないと見えない車に衝突する

## 天候パネル（HORIZON COLOR の明暗と地表の暗さ）

判定は**パネルの明暗メーターの%**（HSLのlは0.10〜0.98に割り当てられているので生の値だとズレる）。

| 明暗メーター  | 地表                                  |
| ------- | ----------------------------------- |
| 50%以上   | 通常どおり                               |
| 50%→10% | %に連動して連続的に暗くなる（`weatherDuskLevel`）  |
| 10%以下   | **Nモードと同じ暗さ**（街灯点灯・白面発光・路面の自己発光も含む） |

- 薄暗さは一段強め（夜の一歩手前まで落ちる）。空だけ暗く地面が昼のまま、という不整合を解消
- **Nモードでは星を必ず表示**（パネルの星スイッチとは独立）
- 首都高の白面（ビル窓・トンネル照明）の発光も `dusk >= 1` で点く

## デモ画面

### 表示時間（1コースあたり）

| コース               | 時間           |
| ----------------- | ------------ |
| 首都高速              | **25秒**（+10） |
| 海岸線 / 森林地帯 / インディ | **21秒**（+6）  |

一周の総時間 60秒 → 88秒。

### ドラッグで鳴り出すエンジン音のリセット（全4曲共通）

デモ中のドラッグ（カメラ操作）はブラウザの音声ロックを解除する（`AUDIO.unlock`）ため、以降エンジン音が鳴り続ける。コース切替＝ページ再読込では消えるが、**曲3は海岸線に留まるので消えない**。そこで「デモがカメラアングルを強制的に切り替える」タイミングで音を無音へ戻す（`AUDIO.silence()`＝鳴っている音を止めて `AudioContext` を suspend。次のドラッグで再び鳴り、また切替で消える）。

| 契機                                | 対象               |
| --------------------------------- | ---------------- |
| 演出によるカメラ視点切替（V相当。海岸線5秒/10秒、森林10秒） | 曲1〜4             |
| 曲3の20秒ごとのアングル切替                   | 曲3               |
| ドラッグ終了2.5秒後にデモがカメラを取り戻した時          | 首都高・森林・インディなど全コース |

- 強制的なV切替が無いコース（首都高・インディ）は3番目の「カメラの取り戻し」で消える
- 判定用に実際にドラッグされた時だけ印を付ける（`cam.lastDrag > 0`）。起動直後の `lastDrag=0` を「今ドラッグした」と数えると、デモ開始1.5秒で無駄なリセットが1回入る
- 観測用：`document.body.dataset.demoEngineSoundResets`（リセット回数）

### BGM 4曲構成（曲がデモの見せ方を決める）

親シェル（`?demoShell=1`）を YouTube IFrame API に切り替え、曲の終了を検知して次へ送る。

| #   | 動画ID        | 曲名                                | 演出                          |
| --- | ----------- | --------------------------------- | --------------------------- |
| 1   | N9JN4aqMduo | Jerry Wallace,Lovers Of The World | 通常デモ（4コース巡回）                |
| 2   | 5igDtWadYms | Eagles,Take it Easy               | 通常デモ                        |
| 3   | I7mI-HYHDbM | Elton John,Understanding Women    | **海岸線のみ・指定の空・20秒ごとにアングル切替** |
| 4   | 4k6YWc59Se4 | 井上陽水,今夜、私に                        | **常に夜(Nモード)**・コースとカメラは通常どおり |

- 曲2〜4は**再生開始3秒後**に、通常の再生中表示と同じ見た目（`♪ 曲名`）で曲名を出す（`setNowPlaying` が♪を自動付与）
- **曲1だけは「デモ突入から12秒後」**（`DEMO_TRACK1_LABEL_AT_MS`）。BGMはデモ突入の7秒後に鳴り始めるので、「再生開始から3秒」だと音が鳴る前に曲名が出てしまう。曲1は再生開始の基準時刻がデモ突入前に確定しないため、突入からの経過時間で数える
- コース切替（ページ再読込）で既に流れている曲を引き継ぐ場合（`labelDelayMs=0`）は、曲1でも音が鳴っている状態なので従来どおり即表示する
- 1曲目のAO3uZdHDpNQは所有者が外部埋め込み禁止のため、同じ曲の埋め込み可能版N9JN4aqMduoを使用
- **ENDEDの重複発火対策**：次の曲が実際にPLAYINGになるまで曲送りを受け付けない（これがないと1曲目の直後に3曲先まで飛び、4曲目だけが残る＝実際に発生した）
- **再生エラー時は1曲目へ戻る**（飛ばさない）。1曲目自体が鳴らない時は往復せず停止（`demoTrackStalled`）
- 曲の演出はコース切替（ページ再読込み）をまたいでも維持（`?demoTrack=N` で引き継ぐ／単体確認にも使える）
- 切り分け用ログ：親フレームの `document.body.dataset.demoTrackLog`（`load1:5igDtWadYms`、`err150@1:...` など）、`demoTrackError`
- ローカル(127.0.0.1)からの埋め込みはYouTubeに弾かれることがある。GitHub Pagesの正式オリジンなら全曲再生できる

### 曲3の空設定

|         | 色調            | 彩度             | 明暗            |
| ------- | ------------- | -------------- | ------------- |
| HORIZON | 10% (h=0.100) | 100% (s=1.000) | 20% (l=0.276) |
| SKY     | 95% (h=0.950) | 95% (s=0.950)  | 35% (l=0.408) |

horizon明暗20%なので薄暗さも連動して dusk=0.75（かなり薄暗い夕景）。海岸線の天候ランダム化（4秒ごと）は曲3では止めている。

## パス構成（GitHub公開）

すべて相対パスで統一済み。親ディレクトリから配信して `/car2026/index.html` として3コース起動し、**失敗リクエスト0・エラー0**を確認。日本語ファイル名（`登場cpu車両list.txt`、`画像/`、`sound/`）も正しく解決される。リポジトリ直下に `car2026/` を置けばそのまま動く。

## 検証環境の注意

- Playwright + SwiftShader は2〜3fpsしか出ず、**FPSは実機の目安にならない**。draw callと三角形数で判断する
- **すれ違い頻度・曲送りはこの環境で実測できない**（60秒でゲーム内128m＝一周の3%しか進まない／YouTubeが再生されない）
- 負荷実測：昼15台 draw62/三角形19万、夜15台 279/20万、インディ10台 draw213/7.6万。10台と15台で描画量がほぼ同じ（フォグと視界で同時描画数が頭打ち）
- 観測用 dataset：`tokyoCpu*`（Encounters/LaneShifts/MinPairGapMeters/ActiveRanks等）、`indyCpu*`（CurrentSpeedsKmh/MaxDriftSpeedKmh/MaxAccel等）、`cpuMaxJumpMeters`、`weatherDuskLevel`、`demoTrack*`、`demoEngineSoundResets`
- URLパラメータ：`?cpuCars=N`（首都高の台数）、`?laneSpacing=`（レーン間隔）、`?demoTrack=N`（デモの曲指定）、`?course=`、`?demo=1`、`?demoShell=1`、`?debugMap=1`
